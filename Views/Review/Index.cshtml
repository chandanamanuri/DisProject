<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Bookish Circle</title>
    <style>
        html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-image: url("/img/nn.jpg"); /* Optional background image */
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            background-attachment: fixed;
        }

        /* Header */
        header {
            background-color: #384b38;
            color: #fff;
            padding: 20px;
            text-align: center;
            height: 75px; /* Set a fixed height */
        }

        h1 {
            font-size: 3em;
            margin-bottom: 10px;
            letter-spacing: 3px;
            text-transform: uppercase;
            font-weight: bold;
            color: #fff;
        }

        /* Navigation */
        nav {
            display: flex;
            justify-content: space-around;
            padding: 10px 0;
            background-color: #c89666;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            clear: both; /* Clear floats */
        }

            nav a {
                color: #384b38;
                text-decoration: none;
                padding: 10px 20px;
                border-radius: 5px;
                transition: background-color 0.3s, color 0.3s;
                font-weight: bold;
            }

                nav a:hover {
                    background-color: #c1a77a;
                    color: #fff;
                }

                nav a.active {
                    background-color: #384b38;
                    color: #fff;
                }

        /* Main content */
        main {
            padding: 20px; /* Add some padding */
        }

        /* Footer */
        footer {
            background-color: #384b38;
            color: #fff;
            padding: 5px;
            text-align: center;
            height: 50px; /* Set a fixed height */
        }


        /* Base styles for the form container */
        .signup-form {
            width: 1200px; /* Adjust width as needed */
            background-color: #384b38; /* Light background for form */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.2); /* Subtle shadow */
            overflow: hidden; /* Prevent content overflow */
            position: relative; /* Allow for after element */
            height: 500px;
        }

            /* Heading styles */
            .signup-form h1 {
                margin-bottom: 20px;
                text-align: center;
                color: #ffffff; /* White text color for headings */
            }

            /* Paragraph styles */
            .signup-form p {
                margin-bottom: 20px;
                text-align: center;
                color: #ffffff; /* White text color for paragraphs */
            }

        /* Form group styles */
        .form-group {
            margin-bottom: 15px;
        }

            /* Label styles */
            .form-group label {
                display: block;
                margin-bottom: 5px;
                color: #ffffff; /* White text color for labels */
                font-size: 1em;
                letter-spacing: 1px; /* Reduced letter spacing for labels */
            }

            /* Input field styles */
            .form-group input {
                display: block;
                width: 100%;
                padding: 10px;
                border: 1px solid #ccc;
                border-radius: 3px;
                box-sizing: border-box; /* Include padding in width calculation */
                transition: border-color 0.3s;
                background-color: #ffffff; /* White background color for input fields */
                color: #384b38; /* Dark text color for input fields */
            }

                /* Input field focus styles */
                .form-group input:focus {
                    border-color: #384b38; /* Night Blue (on focus) */
                }

        /* Button styles */
        button[type="submit"] {
            background-color: #c89666; /* Night Blue */
            color: #384b38; /* Light brown text color for buttons */
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            font-weight: bold;
        }

            /* Button hover styles */
            button[type="submit"]:hover {
                background-color: #c89666;
            }

        /* Background overlay */
        .signup-form::after {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(255, 255, 255, 0.1); /* White overlay (subtle) */
            z-index: -1; /* Place behind form content */
        }

          
        
        
    </style>
</head>
<body>
    <header>
        <h1>The Bookish Circle</h1>
    </header>

    <nav>
        <a asp-controller="Home" asp-action="Index">Home</a>
        <a asp-controller="About" asp-action="Index">About Us</a>
        <a asp-controller="FeaturedBooks" asp-action="Index">FeaturedBooks</a>
        <a asp-controller="Membership" asp-action="Index">Membership</a>
        <a asp-controller="SignUp" asp-action="Index">SignUp</a>
    </nav>

    <main>
        <section class="signup-form">
            <h2>Add a review embark on a literary adventure!</h2>

            <form action="signup.
      <form action="signup.php" method="post">
        <div class="form-group">
          <label for="username">Username:</label>
          <input type="text" id="username" name="username" required>
        </div>
        <div class="form-group">
          <label for="booktitle">Book Title:</label>
          <input type="text" id="booktitle" name="booktitle" required>
        </div>
        <div class="form-group">
          <label for="review">Review:</label>
          <input type="text" id="Review" name="Review" required>
        </div>
        
        <button type="submit">Submit</button>
      </form>
        </section>

    </main>

    <footer>
        <p>&copy; 2024 The Bookish Circle</p>
    </footer>


</body>
</html>
